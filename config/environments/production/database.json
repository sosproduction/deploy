{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-bookshelf",
      "settings": {
        "client": "postgres",
        "host": "${process.env.DATABASE_HOST || 'strapi-database.clkc2h098u1v.us-east-1.rds.amazonaws.com'}",
        "port": "${process.env.DATABASE_PORT || 5432}",
        "database": "${process.env.DATABASE_NAME || 'sos'}",
        "username": "${process.env.DATABASE_USERNAME || 'postgres'}",
        "password": "${process.env.DATABASE_PASSWORD || 'saturn9*123'}"
      },
      "options": {
        "ssl": false
      }
    }
  }
}
